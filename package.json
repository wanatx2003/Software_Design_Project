{
  "name": "software-design-project",
  "version": "1.0.0",
  "description": "Full stack application for Software Design Project",
  "main": "index.js",
  "scripts": {
    "start": "concurrently \"npm run server\" \"npm run client\"",
    "server": "cd backend && npm start",
    "server:dev": "cd backend && npm run dev",
    "client": "cd client && npm start",
    "install:all": "npm install && cd backend && npm install && cd ../client && npm install",
    "test": "concurrently \"cd backend && npm test\" \"cd client && npm test\""
  },
  "dependencies": {
    "concurrently": "^8.2.1",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "mysql": "^2.18.1",
    "nodemon": "^3.1.10",
    "react": "^19.1.1",
    "react-dom": "^19.1.1",
    "react-router-dom": "^7.9.1",
    "react-scripts": "5.0.1"
  }
}
